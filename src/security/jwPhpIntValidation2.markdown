#Judy Weaver
#CS469
#PHP Integer validation code


##<?php
##if(is_int(0-9))
##{
	##echo “is integer\n”
##}
##Else
##{
	##echo “is not an integer\n”
##}

##source: http://www.php.net/manual/en/function.is-int.php

###This code is to validate whether the data is an integer or not. 
It should be inserted into the code wherever a person would like to 
check to make sure an integer is used.
